<mvc:View controllerName="gtsample.controller.View1" xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true" xmlns="sap.m">
    <Page id="page" title="{i18n>title}">
        <content >
            <Panel headerText="{i18n>helloPanelTitle}" class="sapUiResponsiveMargin" width="auto" >
                <content>
                    <Button
                        id="helloDialogButton"
                        text="{i18n>openDialogButtonText}"
                        press=".onOpenDialog"
                        class="sapUiSmallMarginEnd"
                    />
                    <Button
                        id="helloDialogBtn"
                        text="{i18n>openDialogButtonTextP}"
                        press=".onOpenDialogP"
                        class="sapUiSmallMarginEnd"
                    />
                </content>
            </Panel>

            <Panel headerText="{i18n>helloPanelTitle}" class="sapUiResponsiveMargin" width="auto" >
                <content>
                    <Label text = "featureBranch2"/>
                    <Label text = "featureBranch2Label2"/>
                </content>
            </Panel>          
        </content>
    </Page>
</mvc:View>
